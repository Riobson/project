<%= form_for [@project, @ticket] do |f| %>
 
<% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ticket.errors.count, "error") %> prohibited
        this ticket from being saved:
      </h2>
      <ul>
        <% @ticket.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <font color ="black">  <%= f.label :title %> </font><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <font color ="black"><%= f.label :Description %></font><br>
    <%= f.text_area :description %>
  </p>
  <p>
    <font color ="black"><%= f.label :priority %></font><br>
    <%= f.select :priority, [["",""], ["Low", "Low"], ["High", "High"]] %>
  </p>
  <p>
    <font color ="black"><%= f.label :difficulty %></font><br>
    <%= f.select :difficulty, [["",""], ["Easy", "Easy"], ["Medium", "Medium"], ["Hard", "Hard"]] %>
  </p>
  <p>
    <font color ="black"><%= f.label :status %></font><br>
    <%= f.select :status, [["",""], ["Open", "Open"], ["Closed", "Closed"], ["Canceled", "Canceled"]] %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>